import React from 'react';
import PropTypes from 'prop-types';

function Board({ items, status }) {
  // ... mevcut kod devam eder
}

Board.propTypes = {
  items: PropTypes.array.isRequired,
  status: PropTypes.string.isRequired
};

export default Board; 